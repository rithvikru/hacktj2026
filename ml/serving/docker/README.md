# Docker Notes

Use this directory for API and worker container definitions when the backend services are implemented.

Suggested split:

1. `api.Dockerfile`
2. `worker-open-vocab.Dockerfile`
3. `worker-reconstruction.Dockerfile`
4. shared runtime base image
